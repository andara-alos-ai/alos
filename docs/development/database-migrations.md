# Database Migrations

Migration files `001` through `036` are immutable and must not be edited or
renamed. The migration runner records version, name, and checksum.

New migrations use the next sequential number and a domain-based name. Some
early migrations use historical H1-H6 milestone terminology; those names are
preserved deliberately for audit history.
